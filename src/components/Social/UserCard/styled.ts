import styled from "styled-components";

export const StyledUserCard = styled.li`
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-left: 1rem;
    margin-bottom: 1.5rem;
    p {
        opacity: 1;
    }
    img {
        max-width: 76px;
    }
`